const CheckboxInputEditor = () => (
  <div className="xp-formbuilder-editor-checkbox-input-editor">
    <p>Checkbox Input Editor</p>
  </div>
);

CheckboxInputEditor.displayName = "CheckboxInputEditor";

export default CheckboxInputEditor;